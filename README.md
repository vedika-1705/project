steps to run this project are as follows:

1) this project has a server1.js file run that file using command "node server1.js"
2) this will run on localhost:3000
3) you will have to create a mongodb database
4) the project has assests folder which has sign language animated mp4 videos 

